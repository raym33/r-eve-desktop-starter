LM_STUDIO_BASE_URL=http://127.0.0.1:1234/v1
LM_STUDIO_MODEL=local-model
LM_STUDIO_API_KEY=lm-studio
LM_STUDIO_CONTEXT_TOKENS=65536
LM_STUDIO_MAX_OUTPUT_TOKENS=4096

# Pick one real web search provider.
# Recommended private/local option:
# SEARXNG_URL=http://127.0.0.1:8080

# Or API options:
# BRAVE_SEARCH_API_KEY=
# TAVILY_API_KEY=

# Used by Vite dev proxy. Change this if `npm run dev` prints a different Eve port.
VITE_EVE_TARGET=http://127.0.0.1:3000

# raym33/r bridge. Defaults assume the ./r submodule is installed in this starter's .venv.
# R_BRIDGE_PYTHON=/absolute/path/to/.venv/bin/python
# R_BRIDGE_SCRIPT=/absolute/path/to/scripts/r_bridge.py
# R_BRIDGE_ALLOW_DANGEROUS=0
# R_BRIDGE_BLOCKED_SKILLS=android,bluetooth,clipboard,docker,email,gpio,power,realtime_voice,screenshot,social,ssh,voice,wifi
